lab1
====

подсчитывает количество строк, слов и символов

lab2
====

flex2_1.l	выделяет биграммы без учета пробелов и с возвратом символа 
		обратно в поток

flex2_2.l	выделяет биграммы без учета пробелов и без возврата символа
		обратно в поток

flex2_3.l	выделяет биграммы с учетом пробелов и с возвратом символа
		обратно в поток

flex2_4.l	выделяет биграммы с учетом пробелов и с без возврата символа
		обратно в поток
